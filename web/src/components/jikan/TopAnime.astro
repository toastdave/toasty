---

type JikanObject = {
    id: string,
    imageUrl: string,
    titleEnglish: string,
    titleJapanese: string,
    score: number,
}

const pagesToFetch = Number(Astro.props?.pages ?? 5)

let topAnimeList: JikanObject[]

    const response = await fetch('https://api.jikan.moe/v4/top/anime')
    const data = await response.json()



---

<div>
    farts
</div>